{
  "name": "derekbraid.com",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.6.1"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-ga": "^2.1.2",
    "react-router": "^2.8.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && git checkout gh-pages && git add -f build && git commit -am 'Rebuild website prior to deploy' && git filter-branch -f --prune-empty --subdirectory-filter build && git push -f origin gh-pages && git checkout -",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "homepage": "http://derekbraid.com"
}
